services:
  agent:
    build: .
    ports:
      - "8080:8000"
    environment:
      - VLLM_BASE_URL=http://host.docker.internal:8000/v1
      - MODEL=microsoft/Phi-3-mini-4k-instruct
      - OPENAI_API_KEY=none
